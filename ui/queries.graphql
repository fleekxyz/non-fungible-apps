query lastTenMints($skip: Int) {
  newMints(first: 2, skip: $skip, orderDirection: desc, orderBy: tokenId) {
    id
    tokenId
    description
    name
  }
}

query totalTokens {
  tokens {
    id
  }
}

